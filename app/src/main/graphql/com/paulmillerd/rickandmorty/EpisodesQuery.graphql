query EpisodesQuery($page: Int) {
    episodes(page: $page) {
        info {
            pages
        }
        results {
            id
            name
            air_date
            episode
        }
    }
}