query EpisodesQuery($page: Int) {
    episodes(page: $page) {
        info {
            count
        }
        results {
            id
            name
            air_date
            episode
        }
    }
}